{
  "name": "metalborn-webapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npx elm-live ./src/Main.elm --open --proxy-prefix=/api --proxy-host=http://localhost:8081 --dir=./static -- --output=./static/elm.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm-review": "^2.8.4"
  },
  "dependencies": {
    "elm": "^0.19.1-5",
    "elm-format": "^0.8.5",
    "elm-live": "^4.0.2"
  }
}
